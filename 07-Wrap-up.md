
We are mapping real-life objects in programming, and objects show different behavior under different conditions.

Writing code that follows all principles is an ideal thing.

Sometimes you come into a situation where you need to give preference to the business logic over principles.

The principle states that we must use abstraction (abstract classes and interfaces) instead of concrete implementations. The DIP states that:

